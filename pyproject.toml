[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "hundredtrees-api"
authors = [{name = "HundredTrees", email = "hello@hundredtrees.com"}]
dynamic = ["version", "description"]
